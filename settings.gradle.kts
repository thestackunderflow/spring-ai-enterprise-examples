rootProject.name = "spring-ai-enterprise-examples"

// One module per episode. Modules are wired in as each episode's example is built;
// folders for later episodes exist with a README until their code lands.
include(":01-what-is-spring-ai")
