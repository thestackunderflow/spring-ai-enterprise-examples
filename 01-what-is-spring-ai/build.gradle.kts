// Episode 1 — "What Is Spring AI?" — the minimal ChatClient example.
// Boot + Spring AI versions come from the root build's BOMs, so no versions here.
dependencies {
    implementation("org.springframework.ai:spring-ai-starter-model-openai")
}
