// Episode 8 — "MCP with Spring AI" — the same @Tool methods, exposed over the standard MCP protocol.
// The @Tool methods below are what an MCP server exposes; see the README for the mcp-server/-client wiring.
dependencies {
    implementation("org.springframework.ai:spring-ai-starter-model-openai")
}
