spring.application.name=ep08-mcp-integration

spring.ai.openai.api-key=${OPENAI_API_KEY:}
spring.ai.openai.chat.model=gpt-4o-mini

# --- To run this app AS an MCP server (add spring-ai-starter-mcp-server-webmvc) ---
# spring.ai.mcp.server.stdio=true
# spring.ai.mcp.server.protocol=STREAMABLE
# Your @Tool beans (CalculatorService) are then auto-discovered and exposed as MCP tools.
